{
  "name": "fakewebshop-api-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "allure:clean": "rimraf allure-results allure-report",
    "test": "cucumber-js --profile default",
    "test:cucumber": "cucumber-js --profile default",
    "test:allure": "npm run allure:clean && cucumber-js --profile allure",
    "report:allure:generate": "allure generate allure-results --clean -o allure-report",
    "report:allure:open": "allure open allure-report",
    "testrail:check-env": "node -e \"const missing=[]; if(!process.env.TESTRAIL_BASE_URL) missing.push('TESTRAIL_BASE_URL'); if(!process.env.TESTRAIL_USERNAME) missing.push('TESTRAIL_USERNAME'); if(!process.env.TESTRAIL_API_KEY) missing.push('TESTRAIL_API_KEY'); if(missing.length){ console.error('[TestRail] Missing required environment variables for reporting:'); missing.forEach(name => console.error('- ' + name)); process.exit(1); } console.log('[TestRail] Environment is configured for reporting.');\"",
    "test:testrail": "node scripts/run-testrail.js",
    "upload:testrail": "node scripts/upload-testrail.js test-results/cucumber.json"
  },
  "devDependencies": {
    "@brightswagshop/testing-framework": "git+https://github.com/BrightSwagShop/TestingFramework.git#main",
    "@cucumber/cucumber": "^12.7.0",
    "@playwright/test": "^1.54.2",
    "allure-commandline": "^2.34.1",
    "allure-cucumberjs": "^3.4.1",
    "rimraf": "^6.0.1"
  }
}
